[install]
exact = false

[test]
# Browser tests use *.browser.ts extension — bun only auto-discovers *.test.ts / *.spec.ts
root = "."
coverageReporter = ["text"]
coverageThreshold = 0.80

[alias]
"react-devtools-core" = "./src/stubs/react-devtools-core.ts"
