[install]
exact = false

[test]
# Explicit dirs keep bun away from tests/browser/ (those are Playwright-only)
root = "."
coverageReporter = ["text"]
coverageThreshold = 0.80

[alias]
"react-devtools-core" = "./src/stubs/react-devtools-core.ts"
