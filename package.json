{
  "name": "@cortex-ai/cortex",
  "version": "0.1.0",
  "description": "Local-first memory router for AI coding tools",
  "bin": {
    "cortex": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/cortex",
    "test": "bun test tests/e2e tests/router",
    "test:ci": "bun test tests/e2e tests/router --coverage",
    "test:browser": "npx playwright test",
    "test:browser:ui": "npx playwright test --ui",
    "test:all": "bun run test:ci && bun run test:browser"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "boxen": "^7.1.1",
    "chalk": "^5.3.0",
    "chokidar": "^3.6.0",
    "clipboardy": "^4.0.0",
    "hono": "^4.0.0",
    "ink": "^5.0.1",
    "ora": "^8.0.1",
    "react": "^18.2.0",
    "react-devtools-core": "^7.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/react": "^18.2.0",
    "bun-types": "latest",
    "typescript": "^5.3.3"
  }
}
